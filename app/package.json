{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "node node_modules/typescript/bin/tsc --noEmit false --rootDir src --outDir dist && node scripts/copy-static.mjs",
    "smoke:devnet": "node scripts/devnet-smoke.mjs",
    "watch": "esbuild src/main.ts --format=esm --outfile=dist/main.js --watch",
    "typecheck": "node node_modules/typescript/bin/tsc --noEmit",
    "serve": "npx --yes serve ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^26.1.0",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4"
  }
}
